export const metadata = {
  title: "Portail Client",
  description: "Consultez vos factures et devis",
};

export default function PortalLayout({ children }: { children: React.ReactNode }) {
  return children;
}
